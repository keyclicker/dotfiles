# Docker is the sandbox; do not add another permission layer inside it.
approval_policy = "never"
sandbox_mode = "danger-full-access"
model = "gpt-5.6-sol"
model_reasoning_effort = "high"

[features]
default_mode_request_user_input = true
